global:
  scrape_interval: 15s # Par défaut, scrape les cibles toutes les 15 secondes.

scrape_configs:
  - job_name: 'flask.py'
    scrape_interval: 5s
    static_configs:
      - targets: ['127.0.0.1:5000'] # Remplacez ceci par l'adresse de votre application Flask si elle est différente.
